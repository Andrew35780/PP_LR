#include<stdio.h>
#include<string.h>
#include<conio.h>
#include <iostream>
/*
int main()
{
    system("color f0");

    char str[100];
    int i, j, leng, wBegin = 0, wEnd = 0;

    puts("Input string: "); 
    gets_s(str, 100);

    puts(str);
    putchar('\n');

    leng = strlen(str) + 1;
    for (i = 0; i < leng; i++)
    {
        if (str[i] == ' ' || str[i] == '\0')
        {
            wEnd = i;

            if (str[i - 1] == 'k' || str[i - 1] == 'K') { 
                for (j = wBegin; j < wEnd; j++)  
                    printf("%c", str[j]);
                printf("\n");
            }

            wBegin = i + 1;
        }
    }

    return 0;
}
*/