#include <stdio.h> 
#include <iostream>
#include <locale.h>

using namespace std;
/*
void main()
{
    setlocale(LC_CTYPE, "");
    system("color f0");

    float value, newValue, dollarCourse, euroCourse, yuanCourse;
    short code;

    printf("Введите значение в рублях: ");
    scanf_s("%f", &value);
    printf("Введите код валюты (1 2 3): ");
    scanf_s("%hd", &code);

    dollarCourse = 95.03;
    euroCourse = 104.87;
    yuanCourse = 13.48;

    switch (code)
    {
        case 1:
            newValue = value / dollarCourse;
            break;
        case 2:
            newValue = value / euroCourse;
            break;
        case 3:
            newValue = value / yuanCourse;
            break;
        default:
            printf("\nТакого кода валюты не существует\n");
            exit(-1);
    }

    printf("\nКонвертированное значение валюты = %.2lf\n", newValue);
}
*/