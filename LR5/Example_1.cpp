#include <iostream> 
#include <math.h>
#include <locale.h>

using namespace std;

/*
void main()
{
    setlocale(LC_CTYPE, "");
    system("color f0");

    float x, y;

    cout << "\n Введите x:  ";
    cin >> x;

    if (x <= 1)  
        y = 1;
    else if (x < 5)
        y = x * x;
    else 
        y = sqrt(x) + 1;

    cout << " x = " << x << " y = " << y;
}
*/