#include <locale.h>
#include <iostream>
#include <iomanip>
#define n 15 

using namespace std;
/*
void main()
{
	setlocale(LC_CTYPE, "");
	system("color f0");

	int kol, nom, i, a[n];

	cout << "\n";
	for (i = 0; i < n; i++) {
		cout << " Введите элемент массива:" << "\n ";
		cin >> a[i];
	}

	cout << "\n" << " Вывод элементов исходного массива: " << "\n";
	for (i = 0; i < n; i++) cout << setw(6) << a[i];

	cout << "\n" << " Вывод отрицательных элементов массива: " << "\n";
	kol = 0;

	for (i = 0; i < n; i++) { 
		if (a[i] < 0) {
			cout << setw(6) << a[i];
			kol++;
		}
	}
	cout << "\n" << " Количество отрицательных чисел в массиве = " << kol << " \n";
}
*/