#include <iostream>
#include <locale.h>

using namespace std;
/*
void main() {
    setlocale(LC_CTYPE, "");

    system("color f0");

    unsigned long A, B;
    float S, B2;

    cout << "Введите объём информации в Мегабайтах, A=";
    cin >> A;

    cout << "Введите объём информации в Мегабайтах, B=";
    cin >> B;

    A *= 1024 * 1024;
    B2 = B;
    B2 /= 1024;
    
    S = A / 1024.0 + B2 * 1024 * 1024;

    cout.precision(8);

    cout << "\nA=" << A << " байт" << "\nB=" << B2 <<" Гигабайт" << "\nS=" << S << " Килобайт\n";
}
*/