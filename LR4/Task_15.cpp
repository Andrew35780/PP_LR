#include <iostream>
#include <locale.h>
#include <cmath>

using namespace std;
/*
void main() {
    setlocale(LC_CTYPE, "");

    system("color f0");

    float h, S, c, a, b, P;

    cout << "Введите высоту равнобедренного треугольника в мм, h=";
    cin >> h;

    cout << "Введите площадь равнобедренного треугольника в см, S=";
    cin >> S;

    h /= 10.0;

    b = (2 * S) / h;
    a = sqrt(pow(h, 2) + pow((b / 2), 2));
    c = a;

    P = 2 * a + b;

    cout << "\na=" << a << "\nb=" << b << "\nc=" << c << "\nP=" << P << "\n";
}*/
