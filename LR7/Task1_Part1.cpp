#include <iostream>
#include <locale.h>
#include <stdio.h>

/*
void main()
{
    setlocale(LC_CTYPE, "");
    system("color f0");

    float S, P, R;

    printf("\nВведите начальное расстояние (S) в км: ");
    scanf_s("%f", &S);
    printf("Введите процент увеличения (P): ");
    scanf_s("%f", &P);
    printf("Введите целевое расстояние (R) в км: ");
    scanf_s("%f", &R);
    
    int days = 0;
    while (S < R)
    {
        S += S * (P / 100);
        days++;
    }

    printf("\nСпортсмен будет пробегать %.2f км через %d дней\n", R, days);
}
*/