#include <stdio.h>
#include <iostream>

long fact(int n);
/*
int main() {
	setlocale(LC_CTYPE, "");
	system("color f0");

	int number;

	printf("\nВведите число: ");
	scanf_s("%d", &number);

	long result = fact(number);

	printf("Факториал числа %d = %ld\n", number, result);

	return 0;
}

long fact(int n)
{
	if (n <= 1)  
		return 1;
	else  
		return (n * fact(n - 1));
}
*/